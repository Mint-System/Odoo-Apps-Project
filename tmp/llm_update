Look at the git log and the git diff below and reply with a git commit message matching the changes and style of the log.
The commit message must comply with the conventional commits specification. Reply with the commit message only.

<<<  git log -n 5 --pretty=format:"%ci: %B" >>>
2025-09-18 10:31:53 +0200: refactor(helpdesk_project_task): update repository references and dependencies

2025-09-18 10:22:19 +0200: refactor(helpdesk_project_task): simplify notebook XML structure in helpdesk ticket view

2025-09-17 14:30:52 +0200: refactor(helpdesk_project_task): simplify notebook XML structure in project task view

2025-09-17 14:22:06 +0200: feat: add helpdesk_project_task module and update tooling

2025-05-06 12:52:15 +0200: feat(project_phase_estimate): change position

<<< git diff --staged >>>
diff --git a/README.md b/README.md
index d072c35..4cc83c3 100644
--- a/README.md
+++ b/README.md
@@ -13,9 +13,5 @@ git clone git@github.com:Mint-System/Odoo-Apps-project.git ./addons/project
 ## Available modules

 | Module | Summary |
-| --- | --- |
-| [helpdesk_project_task](helpdesk_project_task) |         Link helpdesk tickets to project tasks. |
-| [project_partner_open_tasks](project_partner_open_tasks) |         Access open tasks from partner form. |
-| [project_phase_estimate](project_phase_estimate) |         Estimate planned hours by project and phases. |
-| [project_phase](project_phase) |         Manage project task by project phases. |
-| [project_task_all_menu](project_task_all_menu) |         Add project menu to show all tasks. |
+| ------ | ------- |
+|        |         |
diff --git a/project_phase_estimate/models/project_estimate.py b/project_phase_estimate/models/project_estimate.py
index 04dd0c1..77fad9f 100644
--- a/project_phase_estimate/models/project_estimate.py
+++ b/project_phase_estimate/models/project_estimate.py
@@ -29,11 +29,15 @@ class ProjectEstimate(models.Model):
     @api.depends("project_id", "phase_id", "phase_id.task_ids")
     def _compute_task_ids(self):
         for estimate in self:
-            estimate.task_ids = self.env["project.task"].search(
-                [
-                    ("phase_id", "=", estimate.phase_id.id),
-                    ("project_id", "=", estimate.project_id.id),
-                ]
+            estimate.task_ids = (
+                self.env["project.task"]
+                .with_context(active_test=False)
+                .search(
+                    [
+                        ("phase_id", "=", estimate.phase_id.id),
+                        ("project_id", "=", estimate.project_id.id),
+                    ]
+                )
             )

     planned_date_begin = fields.Datetime("Start Date")
diff --git a/task b/task
index 860dd57..1fa9390 100755
--- a/task
+++ b/task
@@ -17,8 +17,6 @@ function help-table() {
     printf "$COLUMN" "install" "" "Setup the local environment."
     printf "$COLUMN" "lint" "" "Run pre-commit."
     printf "$COLUMN" "docs" "" "Update index.html."
-    printf "$COLUMN" "source" "" "Source the Python virtual env."
-    printf "$COLUMN" "version" "" "Show version of required tools."
 }

 function help() {
@@ -43,10 +41,6 @@ if [ -d "$HOME/taskfile.build/bin" ]; then
     done
 fi

-function version() {
-    uv --version
-}
-
 function install() {
     echo "Setup venv and install python dependencies"
     uv venv env
