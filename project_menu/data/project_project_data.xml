<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="action_project_remove_menu_and_action" model="ir.actions.server">
        <field name="name">Remove Menu and Action</field>
        <field name="model_id" ref="project.model_project_project" />
        <field name="binding_model_id" ref="project.model_project_project" />
        <field name="state">code</field>
        <field name="code">
            if records:
                records._remove_menu_and_action()
        </field>
    </record>

</odoo>
